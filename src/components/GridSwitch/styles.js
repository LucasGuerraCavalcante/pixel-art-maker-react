import { createUseStyles } from 'react-jss';

export default createUseStyles({
    button: {
        margin: '1rem',
        padding: '0.2rem',
        display: 'block',
        width: '100%',
        height: '30px',
        cursor: 'pointer',
        gridArea: 'gridswitch'
    }
});
  